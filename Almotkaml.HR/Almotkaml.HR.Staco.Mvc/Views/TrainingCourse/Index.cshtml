@model TrainingCourseIndexModel

@{
    ViewBag.Title = "التدريب";
}

<br />
<p>
    @Html.Create(Model.CanCreate)
</p>

@Html.Note()

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">@ViewBag.Title</div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <table data-order='[]' class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>ر.ت</th>
                                <th>الاسم</th>
                                <th>الجهة</th>
                                <th>المكان</th>
                                <th>التخصص</th>
                                <th>التاريخ</th>
                                <th>النتيجة</th>
                                <th>@Html.Show(Model.CanEdit)</th>
                                @if (Model.CanDelete)
                                {
                                    <th>حذف</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                        @{
                            var count = 0;
                        }
                        @foreach (var item in Model.TrainingCourseGrid)
                        {
                            count++;
                            <tr class="odd gradeX">
                                <td>@count</td>
                                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                                <td>@Html.DisplayFor(modelItem => item.ExecutingAgency)</td>
                                <td>@Html.DisplayFor(modelItem => item.PlaceCourse)</td>
                                <td>@Html.DisplayFor(modelItem => item.SpecialtyName)</td>
                                <td>@Html.DisplayFor(modelItem => item.Date)</td>
                                <td>@Html.DisplayFor(modelItem => item.Result)</td>
                                <td>
                                    @Html.ActionLink(Html.Show(Model.CanEdit), "Edit", new {id = item.TrainingCourseId}, new {@class = "btn btn-info"})
                                </td>
                                @if (Model.CanDelete)
                                {
                                    <td class="center">@Html.ActionLink("حذف", "Delete", new {id = item.TrainingCourseId}, new {@class = "btn btn-danger"})</td>
                                }
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- /.panel-body -->
        </div>
    </div>
</div>
<!-- /.panel -->
