@using Almotkaml.Extensions
@using Almotkaml.HR
@model TrainingIndexModel

@{
    ViewBag.Title = "التسجيل";
}

<br />
<p>
    @Html.Create(Model.CanCreate)
</p>


@Html.Note()

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">@ViewBag.Title</div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <table data-order='[]' class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>ر.ت</th>
                                <th>نوع التسجيل</th>
                                <th>نوع التطوير</th>
                                <th>المؤهل المطلوب</th>
                                <th>نوع التدريب</th>
                                <th>@Html.Show(Model.CanEdit)</th>
                                @if (Model.CanDelete)
                                {
                                    <th>حذف</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                        @{
                            var count = 0;
                        }
                        @foreach (var item in Model.TrainingGrid)
                        {
                            count++;
                            <tr class="odd gradeX">
                                <td>@count</td>
                                <td>@typeof(TrainingType).DisplayFieldName(item.TrainingType.ToString())</td>
                                <td>@Html.DisplayFor(modelItem => item.DevelopmentTypeDName)</td>
                                <td>@Html.DisplayFor(modelItem => item.RequestedQualificationName)</td>
                                <td>@Html.DisplayFor(modelItem => item.TrainingType)</td>
                                <td>
                                    @Html.ActionLink(Html.Show(Model.CanEdit), "Edit", new {id = item.TrainingId}, new {@class = "btn btn-info"})
                                </td>
                                @if (Model.CanDelete)
                               {
                                    <td class="center">@Html.ActionLink("حذف", "Delete", new {id = item.TrainingId}, new {@class = "btn btn-danger"})</td>
                                }
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- /.panel-body -->
        </div>
    </div>
</div>
<!-- /.panel -->
